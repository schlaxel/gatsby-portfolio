import React from "react";

import Corona from '../components/corona';
import SiteWrapper from '../components/siteWrapperBlank';

const CoronaPage = () => (
    <SiteWrapper withFooter>
        <Corona />
    </SiteWrapper>
);

export default CoronaPage;
