import React from 'react';
import SiteWrapper from '../components/siteWrapper';

const Projekte = () => {
    return (
        <SiteWrapper isPage>
        </SiteWrapper>
    );
};

export default Projekte;